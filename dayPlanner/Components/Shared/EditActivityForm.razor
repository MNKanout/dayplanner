@using dayPlanner.Data.Entities
@using dayPlanner.Services
@using Microsoft.AspNetCore.Components

<div class="activity-form">
    <h4>✏️ Edit Your Activity ✏️</h4>
    <p class="form-intro">Want to change something? No problem! Update the details below.</p>
    <EditForm Model="@Activity" OnValidSubmit="HandleSave">
        <DataAnnotationsValidator />
        
        <div class="mb-3">
            <label class="form-label">📝 What's the Activity? *</label>
            <InputText @bind-Value="Activity.Title" class="form-control" placeholder="e.g., Math Class, Lunch Time, Play Outside" />
            <ValidationMessage For="@(() => Activity.Title)" />
        </div>

        <div class="mb-3">
            <label class="form-label">📍 Where Will This Happen?</label>
            <InputText @bind-Value="Activity.Location" class="form-control" placeholder="e.g., Classroom 3, Cafeteria, Playground" />
        </div>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">🕐 Start Time</label>
                <input type="time" 
                       class="form-control" 
                       value="@GetTimeString(startTime)"
                       @onchange="@((ChangeEventArgs e) => startTime = ParseTime(e.Value?.ToString()))" />
            </div>
            <div class="col-md-6">
                <label class="form-label">🕑 End Time</label>
                <input type="time" 
                       class="form-control" 
                       value="@GetTimeString(endTime)"
                       @onchange="@((ChangeEventArgs e) => endTime = ParseTime(e.Value?.ToString()))" />
            </div>
        </div>

        <div class="mb-3">
            <label class="form-label">🎨 Pick a Fun Icon!</label>
            <div class="icon-selector">
                @foreach (var iconKey in DayPlanConstants.IconKeys)
                {
                    <button type="button"
                            class="icon-button @(Activity.IconKey == iconKey ? "selected" : "")"
                            @onclick="() => Activity.IconKey = iconKey"
                            aria-label="Select @iconKey icon">
                        @GetIconEmoji(iconKey)
                    </button>
                }
            </div>
        </div>

        <div class="mb-3">
            <label class="form-label">🌈 Choose Your Favorite Color!</label>
            <div class="color-selector">
                @foreach (var colorKey in DayPlanConstants.ColorKeys)
                {
                    <button type="button"
                            class="color-button @(Activity.ColorKey == colorKey ? "selected" : "")"
                            style="background-color: @GetColorValue(colorKey)"
                            @onclick="() => Activity.ColorKey = colorKey"
                            aria-label="Select @colorKey color">
                        @if (Activity.ColorKey == colorKey)
                        {
                            <span>✓</span>
                        }
                    </button>
                }
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                <span>💾</span>
                <span>Save Changes</span>
            </button>
            <button type="button" class="btn btn-secondary" @onclick="HandleCancel">Cancel</button>
        </div>
    </EditForm>
</div>

@code {
    [Parameter] public Activity Activity { get; set; } = null!;
    [Parameter] public EventCallback<Activity> OnSave { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private TimeOnly? startTime
    {
        get => Activity.StartTime;
        set => Activity.StartTime = value;
    }

    private TimeOnly? endTime
    {
        get => Activity.EndTime;
        set => Activity.EndTime = value;
    }

    private async Task HandleSave()
    {
        await OnSave.InvokeAsync(Activity);
    }

    private async Task HandleCancel()
    {
        await OnCancel.InvokeAsync();
    }

    private string GetIconEmoji(string iconKey)
    {
        return iconKey switch
        {
            "home" => "🏠",
            "school" => "🏫",
            "meal" => "🍽️",
            "break" => "☕",
            "play" => "🎮",
            "sleep" => "😴",
            _ => "📋"
        };
    }

    private string GetColorValue(string colorKey)
    {
        return colorKey switch
        {
            "blue" => "#3b82f6",
            "green" => "#10b981",
            "yellow" => "#f59e0b",
            "orange" => "#f97316",
            "red" => "#ef4444",
            "purple" => "#a855f7",
            _ => "#6b7280"
        };
    }

    private string GetTimeString(TimeOnly? time)
    {
        return time?.ToString("HH:mm") ?? string.Empty;
    }

    private TimeOnly? ParseTime(string? timeString)
    {
        if (string.IsNullOrEmpty(timeString))
            return null;
        
        if (TimeOnly.TryParse(timeString, out var time))
            return time;
        
        return null;
    }
}

